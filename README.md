multi-tenant logistics management API using Spring Boot that handles the complete order-to-delivery lifecycle. It supports multiple e-commerce platforms, automatically updates order statuses based on fulfillment states, and provides real-time tracking with event history. The system uses RESTful APIs, JPA for data access, and implements idempotent operations for production reliability.
